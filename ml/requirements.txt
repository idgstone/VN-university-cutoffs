# ML study only — NOT needed to reproduce the dataset (core pipeline in ../src is stdlib-only).
# Pinned to the versions the study was run with. torch here is the CPU build.
sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1
numpy==2.5.1
scipy==1.18.0
scikit-learn==1.9.0
pyvi==0.1.1
